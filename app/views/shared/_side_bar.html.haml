%html
  %head
  %body
    .flash-box
    .wrapper
      .side-left
        .left-header
          %p.left-header__user-name 
            = current_user.name
          %ul.user-menu
            %li.user-menu__edit-user
              = link_to new_group_path do
                %i.fa.fa-edit
            %li.user-menu__new-group
              = link_to edit_user_path(current_user) do
                %i.fa.fa-cog
        .groups
          .group
            - current_user.groups.each do |group|
              = link_to group_messages_path(group) do
                .group__group-name 
                  = group.name
                .group__latest-message 
                  メッセージはありません。



-# .side-left
-#   .left-header
-#     %h3.left-header__user-name
-#       = current_user.name
-#       %ul.user-menu
-#         %li.user-menu__edit-user
-#           = link_to new_group_path do
-#             = fa_icon 'pencil-square-o', class: 'icon'
-#         %li.user-menu__new-group
-#           = link_to edit_user_path(current_user) do
-#             = fa_icon 'cog', class: 'icon'
-#       .groups
-#         .group
-#           - current_user.groups.each do |group|
-#             = link_to group_messages_path(group) do
-#               .group__group-name 
-#                 = group.name
-#               .group__latest-message メッセージはまだありません